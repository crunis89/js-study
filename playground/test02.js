// var x = 10;

// // 암묵적 타입 변환
// // 문자열 연결 연산자는 숫자 타입 x의 값을 바탕으로 새로운 문자열을 생성한다.
// var str = x + '';
// console.log(typeof str, str); 

let a = 1, b = 2;

let obj = {
  a: a,
  b: b
};

console.log(obj);