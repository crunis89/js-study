dom 쓰면 html 이 바뀌면 js를 다시 다 건드려야한다(비효율적)


html은 html 요소의 집합체


대표적인 선형구조 ( 배열) 순서대로 이어져 있는거 

Root node ( dom 에서는 document 역할이다


모든 노드들은 style 프로퍼티를 가지고 있다


id가 중복될경우 먼저 선언된 부분이 선택된다


왠만하면 get ~ 블라블라 쓰지말라 getElementsById 제외하고(구형판) 
쿼리~ 블라블라 신형


*단수가 아닌 복수를 선택할때는 자료구조에 담아야 한다(객체, 배열등)
html컬렉션 또는 노드 리스트가 있는데, 배열로 변환해라


자식 노드의 존재 여부는 불리언 값을
반환하는 Node.prototype.hasChildNodes 메소드로 확인할 수 있다.


새니티제이션 함수를 직접 구현할 수도 있겠지만 DOMPurify 라이브러리를 사용하는 것을 추천한다.


벨유 프로퍼티, 어트리뷰터의 차이점을 알아야한다. 같은 속성이 아니다.

어트리뷰티는 바꾸는게 아니다( 초기값개념) / 최신상태값을 바꿔야하느데 
어트리뷰티는 무조건 문자열인데, 프로퍼티값은 문자열이 아닐 수도 있다.








